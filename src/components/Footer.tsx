const Footer = () => (
  <footer className="site-footer">
    <div className="page-shell footer-inner">
      <p>© {new Date().getFullYear()} Rishav Dewan</p>
      <p>Built around proof, not adjectives.</p>
      <a href="#home">Back to top ↑</a>
    </div>
  </footer>
);

export default Footer;
